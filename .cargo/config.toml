[net]
retry = 10
git-fetch-with-cli = true

[http]
timeout = 120
low-speed-limit = 1
multiplexing = false

[build]
# Keep build artifacts local to the workspace for predictable CI and release builds.
target-dir = "target"
